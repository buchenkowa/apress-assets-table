import PropTypes from 'prop-types';

export const urlFieldwPropType = {
  value: PropTypes.string,
  placeholder: PropTypes.string,
  disabled: PropTypes.bool,
  onButtonClick: PropTypes.func,
  onChange: PropTypes.func
};
